<%- include('partials/header') %>

<div class="grid two">
  <div class="card">
    <p class="muted"><%= t('brand') %></p>
    <h1 class="title"><%= t('home_title') %></h1>
    <div class="streak">
      🔥 <%= streak %> <%= lang === 'es' ? 'días' : 'days' %>
      <span class="pill"><%= t('home_subtitle') %></span>
    </div>
    <% if (lastCheck) { %>
      <p class="muted">Last time: <%= lastCheck %> <%= checkedToday ? '(today)' : '' %></p>
    <% } else { %>
      <p class="muted">No sessions yet. Today is a great day to start.</p>
    <% } %>
    <div class="controls" style="margin-top: 12px;">
      <a class="cta" href="/timer"><%= t('home_cta_start') %></a>
      <a class="ghost" href="/install"><%= t('home_cta_install') %></a>
    </div>
  </div>

  <div class="card">
    <h3><%= t('how_it_works') %></h3>
    <ul class="step-list">
      <li class="step-item">1. <%= t('step_calling') %></li>
      <li class="step-item">2. <%= t('step_pray') %></li>
      <li class="step-item">3. <%= t('step_pray_read') %></li>
      <li class="step-item">4. <%= t('step_confession') %></li>
      <li class="step-item">5. <%= t('step_consecration') %></li>
      <li class="step-item">6. <%= t('step_thanks') %></li>
      <li class="step-item">7. <%= t('step_petition') %></li>
    </ul>
  </div>
</div>

<%- include('partials/footer') %>
