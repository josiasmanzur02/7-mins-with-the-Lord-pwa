<!DOCTYPE html>
<html lang="<%= lang %>" data-theme="<%= 'light' %>">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="manifest" href="/manifest.json" />
    <link rel="icon" href="/icons/logo.svg" type="image/svg+xml" />
    <meta name="theme-color" content="#0f766e" />
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600&family=DM+Sans:wght@400;500&display=swap" />
    <link rel="stylesheet" href="/css/style.css" />
    <title>7 Minutes with the Lord</title>
    <script>
      window.I18N_ALL = <%- JSON.stringify(translations) %>;
      window.I18N_LANG = '<%= lang %>';
      window.I18N = window.I18N_ALL[window.I18N_LANG] || window.I18N_ALL.en;
    </script>
  </head>
  <body class="<%= typeof bodyClass !== 'undefined' ? bodyClass : '' %>">
    <div class="shell">
      <header class="topbar">
        <div class="brand">
          <span class="logo-dot"></span>
          <span class="brand-name"><%= t('brand') %></span>
        </div>
        <button class="nav-toggle" aria-label="<%= t('nav_toggle') %>">☰</button>
        <nav class="nav">
          <a href="/home"><%= t('nav_home') %></a>
          <a href="/timer"><%= t('nav_start') %></a>
          <a href="/settings"><%= t('nav_settings') %></a>
          <a href="/install"><%= t('nav_install') %></a>
        </nav>
      </header>

      <% if (flash) { %>
        <div class="flash <%= flash.type %>"><%= flash.message %></div>
      <% } %>

      <main>
