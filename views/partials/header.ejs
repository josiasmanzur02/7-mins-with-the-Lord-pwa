<!DOCTYPE html>
<html lang="<%= lang %>" data-theme="<%= (currentUser && currentUser.theme) || 'light' %>">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="manifest" href="/manifest.json" />
    <link rel="icon" href="/icons/logo.svg" type="image/svg+xml" />
    <meta name="theme-color" content="#0f766e" />
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600&family=DM+Sans:wght@400;500&display=swap" />
    <link rel="stylesheet" href="/css/style.css" />
    <title>7 Minutes with the Lord</title>
  </head>
  <body>
    <div class="shell">
      <header class="topbar">
        <div class="brand">
          <span class="logo-dot"></span>
          <span class="brand-name"><%= t('brand') %></span>
        </div>
        <button class="nav-toggle" aria-label="Toggle menu">☰</button>
        <nav class="nav">
          <% if (currentUser) { %>
            <a href="/home"><%= t('nav_home') %></a>
            <a href="/timer"><%= t('nav_start') %></a>
            <a href="/settings"><%= t('nav_settings') %></a>
            <a href="/install"><%= t('nav_install') %></a>
            <form method="POST" action="/auth/logout" class="inline-form">
              <button type="submit" class="ghost"><%= t('nav_logout') %></button>
            </form>
          <% } else { %>
            <a href="/install"><%= t('nav_install') %></a>
            <a href="/auth"><%= t('nav_login') %></a>
            <a href="/auth?tab=signup"><%= t('nav_signup') %></a>
          <% } %>
        </nav>
      </header>

      <% if (flash) { %>
        <div class="flash <%= flash.type %>"><%= flash.message %></div>
      <% } %>

      <main>
